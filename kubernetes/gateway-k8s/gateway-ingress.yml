apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: gateway
  namespace: jhipster-poc
spec:
  rules:
    - host: gateway.jhipster-poc.spindox.dev
      http:
        paths:
          - path: /
            backend:
              serviceName: gateway
              servicePort: 8080
